# Description
This is on the `branch-name` branch.

For your reference: [SQL Data](https://github.com/NewForce-Cohort-7/tabloidfullstack-agnostic-pillows/tree/main/SQL)

**# Title** in Trello - [Ticket Title](Link trello tidket)
Per the ticket: "Ticket summary here" 

This functionality has now been implemented. 

## Type of change
- [] New feature (non-breaking change which adds functionality)

# Testing Instructions

**GIT this branch:**
1. Clone this repository to your own machine using `git clone https://github.com/NewForce-Cohort-7/tabloidfullstack-agnostic-pillows.git`
2. `git fetch -a` in the main branch to retrieve this branch named `branch-name`.
3.  checkout this branch using the command `git checkout branch-name`


**Create the database:**
1. Open Visual Studio.
2. At the start screen select "Open a Local Folder"
4. Navigate to your workspace directory, open the `tabloidfullstack-agnostic-pinchers` folder, then select the SQL folder
5. Run both .sql files in the [SQL folder](https://github.com/NewForce-Cohort-7/tabloidfullstack-agnostic-pillows/tree/main/SQL) (The first file creates the database and tables if they don’t already exist, and the second file inserts a very small amount of sample data into those tables for testing.).

**Run the app:**
1. Open Visual Studio
2. At the start screen select Open a project or solution.
3. Navigate to your workspace directory, open the `tabloidfullstack-agnostic-pinchers` folder, and select the .sln file to run the solution.
4. In the Solution Explorer in Visual Studio, click on `properties` and into `launchSettings.json`.  Update the applicationUrl under `schema` and `profiles` to "https://localhost:5001". 
5. Hit the Green play button on the toolbar that says TabloidFullStack.
6. This will launch the Swagger page in your default browser. The Swagger page provides the endpoints and data from the SQL server that we created. 
7. Now we need to run the React side of the app. 
8. In your command line, cd into the `client` folder and into the `tabloid` folder. 
9. Start by running `npm install react-scripts`. Once this has been installed, type `npm start`. 
10. This will open the React app in your default browser. You should now see the log-in page. 
11. Log in to user `foo@bar.com`. You do not need to enter a password. 

**Test the new feature:**



# Checklist:
- [] My code follows the style guidelines of this project
- [] I have performed a self-review of my own code
- [] I have commented my code, particularly in hard-to-understand areas
- [] My changes generate no new warnings or errors
- [] I have added test instructions that prove my fix is effective or that my feature works
